{
  "name": "kids-toys-ecommerce-root",
  "version": "1.0.0",
  "description": "Premium Full-Stack Kids Toys E-Commerce Platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run server --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
